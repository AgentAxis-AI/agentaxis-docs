---
title: Tokenomics Model
---

# Tokenomics

## Overview

<div style={{
  padding: '1rem',
  backgroundColor: 'rgba(255, 229, 100, 0.15)',
  borderRadius: '0.5rem',
  marginBottom: '1.5rem',
  border: '1px solid rgba(255, 229, 100, 0.3)'
}}>
  <strong>⚠️ Work in Progress:</strong> We're actively refining our tokenomics model through community feedback. All allocations and percentages shown are preliminary and subject to change.
</div>

**$AXIS** is the native utility token of the Agent Axis project, designed to create a sustainable ecosystem that rewards users and aligns incentives across the platform.

<div style={{
  display: 'flex',
  alignItems: 'center',
  gap: '1rem',
  padding: '0.75rem 1rem',
  margin: '1rem 0',
  backgroundColor: 'rgba(0, 0, 0, 0.2)',
  borderRadius: '0.75rem',
  fontSize: '0.875rem',
}}>
  <span style={{ color: 'var(--fg-light)' }}>Contract Address:</span>
  <code 
    style={{ 
      color: 'var(--fg)',
      fontFamily: 'monospace',
      cursor: 'pointer',
      transition: 'opacity 0.2s ease',
    }}
    onClick={() => {
      navigator.clipboard.writeText('0x0000000000000000000000000000000000000000');
    }}
    onMouseEnter={(e) => {
      e.target.style.opacity = '0.7';
      e.target.title = 'Click to copy';
    }}
    onMouseLeave={(e) => {
      e.target.style.opacity = '1';
    }}
  >Coming soon</code>
  <span style={{ 
    marginLeft: 'auto',
    color: 'var(--fg-light)',
    fontSize: '0.75rem' 
  }}>Solana Network</span>
</div>

## Distribution

<div style={{
  maxWidth: '800px',
  margin: '2rem auto',
  padding: '2rem',
  backgroundColor: 'var(--background)',
  borderRadius: '1.5rem',
}}>
  <div style={{
    display: 'flex',
    flexDirection: 'column',
    gap: '2.5rem',
  }}>
    <div>
      <div style={{
        display: 'flex',
        alignItems: 'center',
        gap: '1rem',
        marginBottom: '0.75rem'
      }}>
        <div style={{
          flex: '1',
          height: '3.5rem',
          backgroundColor: '#60A5FA',
          borderRadius: '1rem',
          display: 'flex',
          alignItems: 'center',
          paddingLeft: '1.5rem',
          color: 'white',
          fontSize: '1.25rem',
          fontWeight: '600'
        }}>
          67%
        </div>
        <div style={{ 
          fontSize: '1.125rem',
          color: 'var(--fg)',
          fontWeight: '500',
          minWidth: '160px' 
        }}>
          Circulating Supply
        </div>
      </div>
      <div style={{ 
        fontSize: '0.9375rem', 
        color: 'var(--fg-light)',
        paddingLeft: '0.5rem' 
      }}>
        Community ownership and fair distribution
      </div>
    </div>

    <div>
      <div style={{
        display: 'flex',
        alignItems: 'center',
        gap: '1rem',
        marginBottom: '0.75rem'
      }}>
        <div style={{
          width: '4rem',
          height: '3.5rem',
          backgroundColor: '#A78BFA',
          borderRadius: '1rem',
          display: 'flex',
          alignItems: 'center',
          justifyContent: 'center',
          color: 'white',
          fontSize: '1.25rem',
          fontWeight: '600'
        }}>
          30%
        </div>
        <div style={{ 
          fontSize: '1.125rem',
          color: 'var(--fg)',
          fontWeight: '500'
        }}>
          Liquidity Locked
        </div>
      </div>
      <div style={{ 
        fontSize: '0.9375rem', 
        color: 'var(--fg-light)',
        paddingLeft: '0.5rem' 
      }}>
        Permanently locked liquidity demonstrating long-term commitment, 
        ensuring community trust and protocol stability.
      </div>
    </div>

    <div>
      <div style={{
        display: 'flex',
        alignItems: 'center',
        gap: '1rem',
        marginBottom: '0.75rem'
      }}>
        <div style={{
          width: '4rem',
          height: '3.5rem',
          backgroundColor: '#F87171',
          borderRadius: '1rem',
          display: 'flex',
          alignItems: 'center',
          justifyContent: 'center',
          color: 'white',
          fontSize: '1.25rem',
          fontWeight: '600'
        }}>
          3%
        </div>
        <div style={{ 
          fontSize: '1.125rem',
          color: 'var(--fg)',
          fontWeight: '500'
        }}>
          Project Fund
        </div>
      </div>
      <div style={{ 
        fontSize: '0.9375rem', 
        color: 'var(--fg-light)',
        paddingLeft: '0.5rem' 
      }}>
        Strategic reserves for ecosystem development and community rewards
      </div>
    </div>
  </div>
</div>

### Circulating Supply (67%)
The majority of tokens will be distributed at launch to ensure wide community ownership and decentralized participation. This fair launch approach creates an inclusive ecosystem where all participants have equal opportunities to engage with the platform.

<div style={{ 
  fontSize: '0.9rem',
  color: 'var(--fg-light)',
  marginTop: '1rem',
  padding: '1rem',
  backgroundColor: 'rgba(0, 0, 0, 0.1)',
  borderRadius: '0.5rem'
}}>
  Token distribution emphasizes decentralized participation through a fair launch model, 
  creating equal opportunities for all ecosystem participants.
</div>

### Ecosystem Growth
<div style={{ 
  padding: '1rem',
  backgroundColor: 'rgba(0, 0, 0, 0.1)',
  borderRadius: '0.5rem',
  margin: '1rem 0'
}}>
  <h4 style={{ marginTop: 0 }}>Coming Soon</h4>
  <p>Strategic allocation details...</p>
</div>

### Liquidity Locking
<div style={{ 
  padding: '1rem',
  backgroundColor: 'rgba(0, 0, 0, 0.1)',
  borderRadius: '0.5rem',
  margin: '1rem 0'
}}>
  <h4 style={{ marginTop: 0 }}>Coming Soon</h4>
  <p>Onchain liquidity management details in final review</p>
</div>

<div style={{ 
  padding: '1rem',
  marginTop: '2rem',
  backgroundColor: 'var(--background)',
  borderRadius: '0.5rem',
  textAlign: 'center'
}}>
  <small>Full tokenomics documentation releasing soon - Updated daily</small>
</div>